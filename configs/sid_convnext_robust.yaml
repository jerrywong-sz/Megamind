experiment: sid_convnext_robust

model_name: convnext_tiny

seed: 42
pretrained: true

image_size: 224
batch_size: 32
epochs: 5

learning_rate: 0.0001
weight_decay: 0.0001

amp: true

train_mode: robust

checkpoint_dir: checkpoints
checkpoint_name: sid_convnext_robust_best.pt
